# To see the GPU's working, run `wsl -list` to see available Linux distributions. 
# Use `wsl -d Ubuntu` to connect to the Ubuntu instance. 
# Run this command to watch the performace of the Nvidia GPU's in the terminal:

watch -n 0.5 nvidia-smi

